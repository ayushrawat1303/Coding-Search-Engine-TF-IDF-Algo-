Delete Duplicate Folders System Due bug there many duplicate folders file system You given array paths where paths array representing absolute path ith folder file system For example represents path onetwothree Two folders not necessarily on same level identical if they contain same nonempty set identical subfolders underlying subfolder structure folders do not need be root level be identical If two more folders identical then mark folders as well as all their subfolders For example folders file structure below identical They as well as their subfolders should all be marked ax axy az bx bxy bz However if file structure also included path bw then folders would not be identical Note ax bx would still be considered identical even with added folder Once all identical folders their subfolders have been marked file system will delete all them file system only runs deletion once so any folders become identical after initial deletion not deleted Return array ans containing paths remaining folders after deleting all marked folders paths may be returned any order Example Input paths Output Explanation file structure as shown Folders their subfolders marked for deletion because they both contain empty folder named Example Input paths Output Explanation file structure as shown Folders abx their subfolders marked for deletion because they both contain empty folder named Note folders identical after deletion but they not deleted because they were not marked beforehand Example Input paths Output Explanation All folders unique file system Note returned array can be different order as order does not matter Constraints pathslength pathslength pathslength sumpathslength path consists lowercase English letters No two paths lead same folder For any folder not root level its parent folder will also be input