Minimum Consecutive Cards Pick Up You given integer array cards where cards represents value ith card pair cards matching if cards have same value Return minimum number consecutive cards you have pick up have pair matching cards among picked cards If it impossible have matching cards return Example Input cards Output Explanation We can pick up cards which contain matching pair cards with value Note picking up cards also optimal Example Input cards Output Explanation There no way pick up set consecutive cards contain pair matching cards Constraints cardslength cards