Rearrange Characters Make Target String You given two indexed strings target You can take some letters from rearrange them form new strings Return maximum number copies target can be formed by taking letters from rearranging them Example Input ilovecodingonleetcode target code Output Explanation For first copy code take letters indices For second copy code take letters indices strings formed ecod code which can both be rearranged into code We can make most two copies code so we return Example Input abcba target abc Output Explanation We can make one copy abc by taking letters indices We can make most one copy abc so we return Note while there extra indices we cannot reuse letter index so we cannot make second copy abc Example Input abbaccaddaeea target aaaaa Output Explanation We can make one copy aaaaa by taking letters indices We can make most one copy aaaaa so we return Constraints slength targetlength target consist lowercase English letters