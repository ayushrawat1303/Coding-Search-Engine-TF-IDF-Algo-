Splitting String Into Descending Consecutive Values You given string consists only digits Check if we can split into two more nonempty substrings such numerical values substrings descending order difference between numerical values every two adjacent substrings equal For example string can be split into with numerical values values descending order adjacent values differ by so this way valid Another example string can be split into However all ways invalid because they have numerical values respectively all which not descending order Return true if it possible split as described above false otherwise substring contiguous sequence characters string Example Input Output false Explanation There no valid way split Example Input Output true Explanation can be split into with numerical values values descending order with adjacent values differing by Example Input Output false Explanation There no valid way split Constraints slength only consists digits