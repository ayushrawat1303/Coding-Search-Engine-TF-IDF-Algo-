Video Stitching You given series video clips from sporting event lasted time seconds These video clips can be overlapping with each other have varying lengths Each video clip described by array clips where clips indicates ith clip started starti ended endi We can cut these clips into segments freely For example clip can be cut into segments Return minimum number clips needed so we can cut clips into segments cover entire sporting event If task impossible return Example Input clips time Output Explanation We take clips total clips Then we can reconstruct sporting event as follows We cut into segments Now we have segments which cover sporting event Example Input clips time Output Explanation We cannot cover with only Example Input clips time Output Explanation We can take clips Constraints clipslength starti endi time