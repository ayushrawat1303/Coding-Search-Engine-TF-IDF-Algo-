Maximum Nesting Depth Two Valid Parentheses Strings string valid parentheses string denoted VPS if only if it consists characters only It empty string It can be written as AB concatenated with where VPSs It can be written as where VPS We can similarly define nesting depth depthS any VPS as follows depth depthA maxdepthA depthB where VPSs depth depthA where VPS For example VPSs with nesting depths not VPSs Given VPS seq split it into two disjoint subsequences such VPSs Alength Blength seqlength Now choose any such such maxdepthA depthB minimum possible value Return answer array length seqlength encodes such choice answer if seq part else answer Note even though multiple answers may exist you may return any them Example Input seq Output Example Input seq Output Constraints seqsize