Flood Fill image represented by integer grid image where image represents pixel value image You also given three integers sr sc color You should perform flood fill on image starting from pixel image perform flood fill consider starting pixel plus any pixels connected directionally starting pixel same color as starting pixel plus any pixels connected directionally those pixels also with same color so on Replace color all aforementioned pixels with color Return modified image after performing flood fill Example Input image sr sc color Output Explanation From center image with position sr sc ie red pixel all pixels connected by path same color as starting pixel ie blue pixels colored with new color Note bottom corner not colored because it not directionally connected starting pixel Example Input image sr sc color Output Explanation starting pixel already colored so no changes made image Constraints imagelength imagelength image color sr sc