Encrypt Decrypt Strings You given character array keys containing unique characters string array values containing strings length You also given another string array dictionary contains all permitted original strings after decryption You should implement data structure can encrypt decrypt indexed string string encrypted with following process For each character string we find index satisfying keys keys Replace with values string Note case character string not present keys encryption process cannot be carried out empty string returned string decrypted with following process For each substring length occurring even index string we find such values If there multiple valid we choose any one them This means string could have multiple possible strings it can decrypt Replace with keys string Implement Encrypter class Encrypterchar keys String values String dictionary Initializes Encrypter class with keys values dictionary String encryptString word Encrypts word with encryption process described above returns encrypted string int decryptString word Returns number possible strings word could decrypt also appear dictionary Example Input Output Explanation Encrypter encrypter new Encrypter encrypterencryptabcd return eizfeiam maps ei maps zf maps ei maps am encrypterdecrypteizfeiam return ei can map zf maps am maps Thus possible strings after decryption abad cbad abcd cbcd those strings abad abcd appear dictionary so answer Constraints keyslength valueslength valueslength dictionarylength dictionarylength All keys dictionary unique wordlength wordlength All word appear keys wordlength even keys values dictionary word word only contain lowercase English letters most calls will be made encrypt decrypt total