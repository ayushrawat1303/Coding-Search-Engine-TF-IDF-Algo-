Open Lock You have lock front you with circular wheels Each wheel has slots wheels can rotate freely wrap around for example we can turn be be Each move consists turning one wheel one slot lock initially starts string representing state wheels You given list deadends dead ends meaning if lock displays any these codes wheels lock will stop turning you will be unable open it Given target representing value wheels will unlock lock return minimum total number turns required open lock if it impossible Example Input deadends target Output Explanation sequence valid moves would be Note sequence like would be invalid because wheels lock become stuck after display becomes dead end Example Input deadends target Output Explanation We can turn last wheel reverse move from Example Input deadends target Output Explanation We cannot reach target without getting stuck Constraints deadendslength deadendslength targetlength target will not be list deadends target deadends consist digits only