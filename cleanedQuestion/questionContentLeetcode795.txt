Snapshot Array Implement SnapshotArray supports following interface SnapshotArrayint length initializes arraylike data structure with given length Initially each element equals void setindex val sets element given index be equal val int snap takes snapshot array returns snap_id total number times we called snap minus int getindex snap_id returns value given index time we took snapshot with given snap_id Example Input Output Explanation SnapshotArray snapshotArr new SnapshotArray set length be snapshotArrset Set array snapshotArrsnap Take snapshot return snap_id snapshotArrset snapshotArrget Get value array with snap_id return Constraints length index length val snap_id total number times we call snap most calls will be made set snap get