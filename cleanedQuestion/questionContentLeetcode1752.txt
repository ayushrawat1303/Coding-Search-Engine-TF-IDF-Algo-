Number Valid Clock Times You given string length called time representing current time on digital clock format hhmm earliest possible time latest possible time string time digits represented by symbol unknown must be replaced with digit from Return integer answer number valid clock times can be created by replacing every with digit from Example Input time Output Explanation We can replace with either producing Note we cannot replace it with since time invalid total we have two choices Example Input time Output Explanation Each can be replaced by any digit from so we have total choices Example Input time Output Explanation There possible choices for hours possible choices for minutes total we have choices Constraints time valid string length format hhmm hh mm Some digits might be replaced with need be replaced with digits from