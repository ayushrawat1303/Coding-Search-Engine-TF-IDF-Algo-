Minimum Operations Reduce Integer You given positive integer you can do following operation any number times Add subtract power from Return minimum number operations make equal number power if where Example Input Output Explanation We can do following operations Add so now Subtract from so now Subtract from so now It can be shown minimum number operations we need make equal Example Input Output Explanation We can do following operations Add so now Add so now Subtract from so now So minimum number operations Constraints