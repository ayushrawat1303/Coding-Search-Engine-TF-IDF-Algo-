Check if Parentheses String Can Be Valid parentheses string nonempty string consisting only It valid if any following conditions true It It can be written as AB concatenated with where valid parentheses strings It can be written as where valid parentheses string You given parentheses string string locked both length locked binary string consisting only For each index locked If locked you cannot change But if locked you can change either Return true if you can make valid parentheses string Otherwise return false Example Input locked Output true Explanation locked locked so we cannot change We change while leaving unchanged make valid Example Input locked Output true Explanation We do not need make any changes because already valid Example Input locked Output false Explanation locked permits us change Changing either will not make valid Constraints slength lockedlength either locked either