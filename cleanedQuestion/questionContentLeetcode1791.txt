Delete Greatest Value Each Row You given matrix grid consisting positive integers Perform following operation until grid becomes empty Delete element with greatest value from each row If multiple such elements exist delete any them Add maximum deleted elements answer Note number columns decreases by one after each operation Return answer after performing operations described above Example Input grid Output Explanation diagram above shows removed values each step first operation we remove from first row from second row notice there two cells with value we can remove any them We add answer second operation we remove from first row from second row We add answer third operation we remove from first row from second row We add answer final answer Example Input grid Output Explanation diagram above shows removed values each step first operation we remove from first row We add answer final answer Constraints gridlength gridlength grid