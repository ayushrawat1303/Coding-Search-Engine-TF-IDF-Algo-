Replace All Digits with Characters You given indexed string has lowercase English letters its even indices digits its odd indices There function shiftc where character digit returns xth character after For example shifta shiftx For every odd index you want replace digit with shifts Return after replacing all digits It guaranteed shifts will never exceed Example Input ace Output abcdef Explanation digits replaced as follows shifta shiftc shifte Example Input abcde Output abbdcfdhe Explanation digits replaced as follows shifta shiftb shiftc shiftd Constraints slength consists only lowercase English letters digits shifts for all odd indices