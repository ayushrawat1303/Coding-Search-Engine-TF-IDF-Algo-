All Oone Data Structure Design data structure store strings count with ability return strings with minimum maximum counts Implement AllOne class AllOne Initializes object data structure incString key Increments count string key by If key does not exist data structure insert it with count decString key Decrements count string key by If count key after decrement remove it from data structure It guaranteed key exists data structure before decrement getMaxKey Returns one keys with maximal count If no element exists return empty string getMinKey Returns one keys with minimum count If no element exists return empty string Note each function must run average time complexity Example Input Output Explanation AllOne allOne new AllOne allOneinchello allOneinchello allOnegetMaxKey return hello allOnegetMinKey return hello allOneincleet allOnegetMaxKey return hello allOnegetMinKey return leet Constraints keylength key consists lowercase English letters It guaranteed for each call dec key existing data structure most calls will be made inc dec getMaxKey getMinKey