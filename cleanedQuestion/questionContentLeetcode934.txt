Minimum Moves Move Box Their Target Location storekeeper game which player pushes boxes around warehouse trying get them target locations game represented by grid characters grid where each element wall floor box Your task move box target position under following rules character represents player player can move up down left right grid if it floor empty cell character represents floor which means free cell walk character represents wall which means obstacle impossible walk there There only one box one target cell grid box can be moved adjacent free cell by standing next box then moving direction box This push player cannot walk through box Return minimum number pushes move box target If there no way reach target return Example Input grid Output Explanation We return only number times box pushed Example Input grid Output Example Input grid Output Explanation push box down left left up up Constraints gridlength gridlength grid contains only characters There only one character grid