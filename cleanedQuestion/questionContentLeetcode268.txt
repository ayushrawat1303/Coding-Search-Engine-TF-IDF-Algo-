Count Repetitions We define str as string str which consists string concatenated times For example str abcabcabc We define string can be obtained from string if we can remove some characters from such it becomes For example abc can be obtained from abdbec based on our definition by removing bolded underlined characters You given two strings two integers You have two strings str str Return maximum integer such str can be obtained from str Example Input acb ab Output Example Input acb acb Output Constraints slength slength consist lowercase English letters