Min Max Game You given indexed integer array nums whose length power Apply following algorithm on nums Let be length nums If end process Otherwise create new indexed integer array newNums length For every even index where assign value newNums as minnums nums For every odd index where assign value newNums as maxnums nums Replace array nums with newNums Repeat entire process starting from step Return last number remains nums after applying algorithm Example Input nums Output Explanation following arrays results applying algorithm repeatedly First nums Second nums Third nums last remaining number so we return Example Input nums Output Explanation already last remaining number so we return Constraints numslength nums numslength power