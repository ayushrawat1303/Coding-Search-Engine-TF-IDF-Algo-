Minimum Moves Convert String You given string consisting characters which either move defined as selecting three consecutive characters converting them Note if move applied character it will stay same Return minimum number moves required so all characters converted Example Input XXX Output Explanation XXX OOO We select all characters convert them one move Example Input XXOX Output Explanation XXOX OOOX OOOO We select first characters first move convert them Then we select last characters convert them so final string contains all Os Example Input OOOO Output Explanation There no Xs convert Constraints slength either