Detect Cycles Grid Given array characters grid size you need find if there exists any cycle consisting same value grid cycle path length more grid starts ends same cell From given cell you can move one cells adjacent it one four directions up down left right if it has same value current cell Also you cannot move cell you visited your last move For example cycle invalid because from we visited which was last visited cell Return true if any cycle same value exists grid otherwise return false Example Input grid Output true Explanation There two valid cycles shown different colors image below Example Input grid Output true Explanation There only one valid cycle highlighted image below Example Input grid Output false Constraints gridlength gridlength grid consists only lowercase English letters