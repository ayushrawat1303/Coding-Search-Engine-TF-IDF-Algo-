Shifting Letters You given string lowercase English letters integer array shifts same length Call shift letter next letter alphabet wrapping around so becomes For example shifta shiftt shiftz Now for each shifts we want shift first letters times Return final string after all such shifts applied Example Input abc shifts Output rpl Explanation We start with abc After shifting first letters by we have dbc After shifting first letters by we have igc After shifting first letters by we have rpl answer Example Input aaa shifts Output gfd Constraints slength consists lowercase English letters shiftslength slength shifts