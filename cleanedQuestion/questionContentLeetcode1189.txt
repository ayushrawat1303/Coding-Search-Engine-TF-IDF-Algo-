Maximum Nesting Depth Parentheses string valid parentheses string denoted VPS if it meets one following It empty string single character not equal It can be written as AB concatenated with where VPSs It can be written as where VPS We can similarly define nesting depth depthS any VPS as follows depth depthC where string with single character not equal depthA maxdepthA depthB where VPSs depth depthA where VPS For example VPSs with nesting depths not VPSs Given VPS represented as string return nesting depth Example Input Output Explanation Digit inside nested parentheses string Example Input Output Constraints slength consists digits characters It guaranteed parentheses expression VPS