Maximum You given indexed integer array nums length integer operation you can choose element multiply it by Return maximum possible value nums nums nums can be obtained after applying operation on nums most times Note denotes bitwise between two integers Example Input nums Output Explanation If we apply operation index our new array nums will be equal Thus we return bitwise which Example Input nums Output Explanation If we apply operation twice on index we yield new array Thus we return Constraints numslength nums