Split String Balanced Strings Balanced strings those have equal quantity characters Given balanced string split it into some number substrings such Each substring balanced Return maximum number balanced strings you can obtain Example Input RLRRLLRLRL Output Explanation can be split into RL RRLL RL RL each substring contains same number Example Input RLRRRLLRLL Output Explanation can be split into RL RRRLLRLL each substring contains same number Note cannot be split into RL RR RL LR LL because nd th substrings not balanced Example Input LLLLRRRR Output Explanation can be split into LLLLRRRR Constraints slength either balanced string