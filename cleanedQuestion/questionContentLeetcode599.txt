Reveal Cards Increasing Order You given integer array deck There deck cards where every card has unique integer integer on ith card deck You can order deck any order you want Initially all cards start face down unrevealed one deck You will do following steps repeatedly until all cards revealed Take top card deck reveal it take it out deck If there still cards deck then put next top card deck bottom deck If there still unrevealed cards go back step Otherwise stop Return ordering deck would reveal cards increasing order Note first entry answer considered be top deck Example Input deck Output Explanation We get deck order this order does not matter reorder it After reordering deck starts as where top deck We reveal move bottom deck now We reveal move bottom deck now We reveal move bottom deck now We reveal move bottom deck now We reveal move bottom deck now We reveal move bottom deck now We reveal Since all cards revealed increasing order answer correct Example Input deck Output Constraints decklength deck All values deck unique