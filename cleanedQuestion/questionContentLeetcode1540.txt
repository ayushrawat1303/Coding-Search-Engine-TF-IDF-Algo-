Check if All As Appears Before All Bs Given string consisting only characters return true if every appears before every string Otherwise return false Example Input aaabbb Output true Explanation as indices while bs indices Hence every appears before every we return true Example Input abab Output false Explanation There index index Hence not every appears before every we return false Example Input bbb Output true Explanation There no as hence every appears before every we return true Constraints slength either