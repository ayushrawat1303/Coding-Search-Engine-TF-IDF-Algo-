Binary Tree Cameras You given root binary tree We install cameras on tree nodes where each camera node can monitor its parent itself its immediate children Return minimum number cameras needed monitor all nodes tree Example Input root Output Explanation One camera enough monitor all nodes if placed as shown Example Input root Output Explanation least two cameras needed monitor all nodes tree above image shows one valid configurations camera placement Constraints number nodes tree range Nodeval