Find Width Columns Grid You given indexed integer matrix grid width column maximum length its integers For example if grid width only column since length Return integer array ans size where ans width ith column length integer with len digits equal len if nonnegative len otherwise Example Input grid Output Explanation th column length Example Input grid Output Explanation th column only length st column all integers length nd column both length Constraints gridlength gridlength grid