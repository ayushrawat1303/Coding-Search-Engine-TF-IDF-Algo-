Minimum Obstacle Removal Reach Corner You given indexed integer array grid size Each cell has one two values represents empty cell represents obstacle may be removed You can move up down left right from empty cell Return minimum number obstacles remove so you can move from upper left corner lower right corner Example Input grid Output Explanation We can remove obstacles create path from It can be shown we need remove least obstacles so we return Note there may be other ways remove obstacles create path Example Input grid Output Explanation We can move from without removing any obstacles so we return Constraints gridlength gridlength grid either grid grid