Verbal Arithmetic Puzzle Given equation represented by words on left side result on right side You need check if equation solvable under following rules Each character decoded as one digit No two characters can map same digit Each words result decoded as one number without leading zeros Sum numbers on left side words will equal number on right side result Return true if equation solvable otherwise return false Example Input words result MONEY Output true Explanation Map Such SEND MORE MONEY Example Input words result TWENTY Output true Explanation Map Such SIX SEVEN SEVEN TWENTY Example Input words result POINT Output false Explanation There no possible mapping satisfy equation so we return false Note two different characters cannot map same digit Constraints wordslength wordslength resultlength words result contain only uppercase English letters number different characters used expression most