Minimum String Length After Removing Substrings You given string consisting only uppercase English letters You can apply some operations this string where one operation you can remove any occurrence one substrings AB CD from Return minimum possible length resulting string you can obtain Note string concatenates after removing substring could produce new AB CD substrings Example Input ABFCACDB Output Explanation We can do following operations Remove substring ABFCACDB so FCACDB Remove substring FCACDB so FCAB Remove substring FCAB so FC So resulting length string It can be shown it minimum length we can obtain Example Input ACBBD Output Explanation We cannot do any operations on string so length remains same Constraints slength consists only uppercase English letters