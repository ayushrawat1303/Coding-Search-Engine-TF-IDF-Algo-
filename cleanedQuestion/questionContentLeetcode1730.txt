Time Needed Rearrange Binary String You given binary string one second all occurrences simultaneously replaced with This process repeats until no occurrences exist Return number seconds needed complete this process Example Input Output Explanation After one second becomes After another second becomes After third second becomes After fourth second becomes No occurrence exists any longer process needed seconds complete so we return Example Input Output Explanation No occurrence exists processes needed seconds complete so we return Constraints slength either Follow up Can you solve this problem On time complexity