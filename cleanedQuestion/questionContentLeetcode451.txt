Kth Symbol Grammar We build table rows indexed We start by writing st row Now every subsequent row we look previous row replace each occurrence with each occurrence with For example for st row nd row rd row Given two integer return kth indexed symbol nth row table rows Example Input Output Explanation row Example Input Output Explanation row row Example Input Output Explanation row row Constraints