Maximum Points Archery Competition Alice Bob opponents archery competition competition has set following rules Alice first shoots numArrows arrows then Bob shoots numArrows arrows points then calculated as follows target has integer scoring sections ranging from inclusive For each section target with score between say Alice Bob have shot ak bk arrows on section respectively If ak bk then Alice takes points If ak bk then Bob takes points However if ak bk then nobody takes points For example if Alice Bob both shot arrows on section with score then Alice takes points On other hand if Alice shot arrows on section with score Bob shot arrows on same section then Bob takes points You given integer numArrows integer array aliceArrows size which represents number arrows Alice shot on each scoring section from Now Bob wants maximize total number points he can obtain Return array bobArrows which represents number arrows Bob shot on each scoring section from sum values bobArrows should equal numArrows If there multiple ways for Bob earn maximum total points return any one them Example Input numArrows aliceArrows Output Explanation table above shows how competition scored Bob earns total point It can be shown Bob cannot obtain score higher than points Example Input numArrows aliceArrows Output Explanation table above shows how competition scored Bob earns total point It can be shown Bob cannot obtain score higher than points Constraints numArrows aliceArrowslength bobArrowslength aliceArrows bobArrows numArrows sumaliceArrows numArrows