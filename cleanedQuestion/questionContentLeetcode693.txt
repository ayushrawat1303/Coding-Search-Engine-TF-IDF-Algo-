Tallest Billboard You installing billboard want it have largest height billboard will have two steel supports one on each side Each steel support must be equal height You given collection rods can be welded together For example if you have rods lengths you can weld them together make support length Return largest possible height your billboard installation If you cannot support billboard return Example Input rods Output Explanation We have two disjoint subsets which have same sum Example Input rods Output Explanation We have two disjoint subsets which have same sum Example Input rods Output Explanation billboard cannot be supported so we return Constraints rodslength rods sumrods