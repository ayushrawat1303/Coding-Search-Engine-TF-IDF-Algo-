Match Substring After Replacement You given two strings sub You also given character array mappings where mappings indicates you may perform following operation any number times Replace character oldi sub with newi Each character sub cannot be replaced more than once Return true if it possible make sub substring by replacing zero more characters according mappings Otherwise return false substring contiguous nonempty sequence characters within string Example Input foolebar sub leet mappings Output true Explanation Replace first sub with sub with Now sub le substring so we return true Example Input fooleetbar sub fl mappings Output false Explanation string fl not substring no replacements can be made Note we cannot replace with Example Input FooltbaR sub leetd mappings Output true Explanation Replace first second sub with sub with Now sub ltb substring so we return true Constraints sublength slength mappingslength mappingslength oldi newi sub consist uppercase lowercase English letters digits oldi newi either uppercase lowercase English letters digits