Number Ways Divide Long Corridor Along long library corridor there line seats decorative plants You given indexed string corridor length consisting letters where each represents seat each represents plant One room divider has already been installed left index another right index Additional room dividers can be installed For each position between indices most one divider can be installed Divide corridor into nonoverlapping sections where each section has exactly two seats with any number plants There may be multiple ways perform division Two ways different if there position with room divider installed first way but not second way Return number ways divide corridor Since answer may be very large return it modulo If there no way return Example Input corridor SSPPSPS Output Explanation There different ways divide corridor black bars above image indicate two room dividers already installed Note each ways each section has exactly two seats Example Input corridor PPSPSP Output Explanation There only way divide corridor by not installing any additional dividers Installing any would create some section does not have exactly two seats Example Input corridor Output Explanation There no way divide corridor because there will always be section does not have exactly two seats Constraints corridorlength corridor either