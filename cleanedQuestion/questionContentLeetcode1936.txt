Convert Array Into Array With Conditions You given integer array nums You need create array from nums satisfying following conditions array should contain only elements array nums Each row array contains distinct integers number rows array should be minimal Return resulting array If there multiple answers return any them Note array can have different number elements on each row Example Input nums Output Explanation We can create array contains following rows All elements nums were used each row array contains distinct integers so it valid answer It can be shown we cannot have less than rows valid array Example Input nums Output Explanation All elements array distinct so we can keep all them first row array Constraints numslength nums numslength