Largest Number After Mutating Substring You given string num which represents large integer You also given indexed integer array change length maps each digit another digit More formally digit maps digit change You may choose mutate single substring num mutate substring replace each digit num with digit it maps change ie replace num with change Return string representing largest possible integer after mutating choosing not single substring num substring contiguous sequence characters within string Example Input num change Output Explanation Replace substring maps change Thus becomes largest number can be created so return it Example Input num change Output Explanation Replace substring maps change maps change maps change Thus becomes largest number can be created so return it Example Input num change Output Explanation already largest number can be created so return it Constraints numlength num consists only digits changelength change