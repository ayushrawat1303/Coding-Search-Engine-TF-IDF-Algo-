Sum Scores Built Strings You building string length one character time prepending each new character front string strings labeled from where string with length labeled si For example for abaca ca aca etc score si length longest common prefix between si sn Note sn Given final string return sum score every si Example Input babab Output Explanation For longest common prefix which has score For ab there no common prefix so score For bab longest common prefix bab which has score For abab there no common prefix so score For babab longest common prefix babab which has score sum scores so we return Example Input azbazbzaz Output Explanation For az longest common prefix az which has score For azbzaz longest common prefix azb which has score For azbazbzaz longest common prefix azbazbzaz which has score For all other si score sum scores so we return Constraints slength consists lowercase English letters