KSimilar Strings Strings ksimilar for some nonnegative integer if we can swap positions two letters exactly times so resulting string equals Given two anagrams return smallest for which ksimilar Example Input ab ba Output Explanation two string similar because we can use one swap change ab ba Example Input abc bca Output Explanation two strings similar because we can use two swaps change abc bac bca Constraints slength slength slength contain only lowercase letters from set anagram