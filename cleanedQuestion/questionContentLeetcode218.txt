Binary Watch binary watch has LEDs on top represent hours LEDs on bottom represent minutes Each LED represents zero one with least significant bit on right For example below binary watch reads Given integer turnedOn which represents number LEDs currently on ignoring PM return all possible times watch could represent You may return answer any order hour must not contain leading zero For example not valid It should be minute must be consist two digits may contain leading zero For example not valid It should be Example Input turnedOn Output Example Input turnedOn Output Constraints turnedOn