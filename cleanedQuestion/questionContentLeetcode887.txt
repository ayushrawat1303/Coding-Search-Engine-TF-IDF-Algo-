Maximum Equal Frequency Given array nums positive integers return longest possible length array prefix nums such it possible remove exactly one element from this prefix so every number has appeared it will have same number occurrences If after removing one element there no remaining elements its still considered every appeared number has same number ocurrences Example Input nums Output Explanation For subarray length if we remove nums we will get so each number will appear exactly twice Example Input nums Output Constraints numslength nums