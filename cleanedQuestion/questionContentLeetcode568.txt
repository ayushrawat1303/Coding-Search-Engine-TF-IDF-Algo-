Super Egg Drop You given identical eggs you have access building with floors labeled from You know there exists floor where such any egg dropped floor higher than will break any egg dropped below floor will not break Each move you may take unbroken egg drop it from any floor where If egg breaks you can no longer use it However if egg does not break you may reuse it future moves Return minimum number moves you need determine with certainty what value Example Input Output Explanation Drop egg from floor If it breaks we know Otherwise drop egg from floor If it breaks we know If it does not break then we know Hence we need minimum moves determine with certainty what value Example Input Output Example Input Output Constraints