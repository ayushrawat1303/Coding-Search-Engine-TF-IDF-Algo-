Least Operators Express Number Given single positive integer we will write expression form op op op where each operator op op etc either addition subtraction multiplication division For example with we might write which value When writing such expression we adhere following conventions division operator returns rational numbers There no parentheses placed anywhere We use usual order operations multiplication division happen before addition subtraction It not allowed use unary negation operator For example valid expression as it only uses subtraction but not because it uses negation We would like write expression with least number operators such expression equals given target Return least number operators used Example Input target Output Explanation expression contains operations Example Input target Output Explanation expression contains operations Example Input target Output Explanation expression contains operations Constraints target