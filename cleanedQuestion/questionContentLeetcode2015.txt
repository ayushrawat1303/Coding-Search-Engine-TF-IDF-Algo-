Number Adjacent Elements With Same Color There indexed array nums length Initially all elements uncolored has value You given integer array queries where queries For each query you color index indexi with color colori array nums Return array answer same length as queries where answer number adjacent elements with same color after ith query More formally answer number indices such nums nums nums after ith query Example Input queries Output Explanation Initially array nums where denotes uncolored elements array After st query nums count adjacent elements with same color After nd query nums count adjacent elements with same color After rd query nums count adjacent elements with same color After th query nums count adjacent elements with same color After th query nums count adjacent elements with same color Example Input queries Output Explanation Initially array nums where denotes uncolored elements array After st query nums count adjacent elements with same color Constraints querieslength querieslength indexi colori