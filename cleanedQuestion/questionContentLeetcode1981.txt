Find Prefix Common Array Two Arrays You given two indexed integer permutations length prefix common array array such equal count numbers present before index both Return prefix common array sequence integers called permutation if it contains all integers from exactly once Example Input Output Explanation no number common so common so common so common so Example Input Output Explanation no number common so only common so common so Constraints Alength Blength It guaranteed both permutation integers