Partition String Into Substrings With Values Most You given string consisting digits from integer partition string called good if Each digit part exactly one substring value each substring less than equal Return minimum number substrings good partition If no good partition exists return Note value string its result when interpreted as integer For example value value substring contiguous sequence characters within string Example Input Output Explanation We can partition string into substrings Each substring has value less than equal It can be shown we cannot partition string into less than substrings Example Input Output Explanation There no good partition for this string Constraints slength digit from