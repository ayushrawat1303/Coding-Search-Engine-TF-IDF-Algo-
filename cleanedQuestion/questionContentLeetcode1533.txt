Count Words Obtained After Adding Letter You given two indexed arrays strings startWords targetWords Each string consists lowercase English letters only For each string targetWords check if it possible choose string from startWords perform conversion operation on it be equal from targetWords conversion operation described following two steps Append any lowercase letter not present string its end For example if string abc letters can be added it but not If added resulting string will be abcd Rearrange letters new string any arbitrary order For example abcd can be rearranged acbd bacd cbda so on Note it can also be rearranged abcd itself Return number strings targetWords can be obtained by performing operations on any string startWords Note you will only be verifying if string targetWords can be obtained from string startWords by performing operations strings startWords do not actually change during this process Example Input startWords targetWords Output Explanation order form targetWords tack we use startWords act append it rearrange actk tack There no string startWords can be used obtain targetWords act Note act does exist startWords but we must append one letter string before rearranging it order form targetWords acti we use startWords act append it rearrange acti acti itself Example Input startWords targetWords Output Explanation order form targetWords abc we use startWords ab add it rearrange it abc There no string startWords can be used obtain targetWords abcd Constraints startWordslength targetWordslength startWordslength targetWordslength Each string startWords targetWords consists lowercase English letters only No letter occurs more than once any string startWords targetWords