GCD Sort Array You given integer array nums you can perform following operation any number times on nums Swap positions two elements nums nums if gcdnums nums where gcdnums nums greatest common divisor nums nums Return true if it possible sort nums nondecreasing order using above swap method false otherwise Example Input nums Output true Explanation We can sort by performing following operations Swap because gcd nums Swap because gcd nums Example Input nums Output false Explanation It impossible sort array because cannot be swapped with any other element Example Input nums Output true We can sort by performing following operations Swap because gcd nums Swap because gcd nums Swap because gcd nums Constraints numslength nums